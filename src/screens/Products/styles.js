import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  itemContainer: {
    padding: 1,
    width: "50%",
  },
  image: {
    aspectRatio: 1,
    width: "100%",
  },
});

export default styles;
